{
  "name": "otel-astro-shop-qa-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Automated test suite for the OpenTelemetry Astronomy Shop checkout, cart, and product-catalog flows",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api",
    "test:e2e": "playwright test tests/e2e",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
